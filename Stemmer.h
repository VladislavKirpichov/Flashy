#include <vector>
#include <string>

//Выделяет главную часть слова (уберает окончания, суффиксы)
class Stemmer {
public:
    // std::vector<std::string> operator() () const {
    //     return std::vector<std::string>();
    // }
    
    //Выделяет главную часть слова (уберает окончания, суффиксы)
    std::vector<std::string> stem() {
        return std::vector<std::string>();
    }
};